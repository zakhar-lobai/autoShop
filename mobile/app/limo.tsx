import RentalWithDriverScreen from './rental-with-driver';

// Limo page shares the same content/sections as Rental with Driver
export default function LimoScreen() {
  return <RentalWithDriverScreen />;
}
